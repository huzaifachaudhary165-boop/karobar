{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "//": [
    "bom1 is Mumbai, the same AWS region as this project's Supabase database",
    "(aws-1-ap-south-1). Keeping the function there is the single largest",
    "performance decision in this project.",
    "",
    "The function was running in iad1 (Washington DC), so every query crossed",
    "the planet. Measured against the deployment: the function answers a route",
    "with no database work in ~560ms; one database round trip took it to ~3.6s;",
    "a list endpoint ~6.8s; the dashboard, which costs 27 queries, ~18.6s.",
    "That is ~3s to open a connection plus ~570ms per query, and NullPool means",
    "the connection is opened per request rather than reused. In-region both",
    "collapse to single-digit milliseconds.",
    "",
    "IMPORTANT: on the Hobby plan this key is IGNORED. The region has to be set",
    "in the Vercel dashboard under Settings -> Functions -> Function Region, and",
    "a redeploy is needed for it to take effect. It is kept here because it",
    "records the intent, and because it does take effect on Pro.",
    "",
    "If the database is ever moved, move this with it."
  ],
  "regions": ["bom1"],
  "functions": {
    "api/index.py": {
      "memory": 1024,
      "maxDuration": 60,
      "includeFiles": "backend/**"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index/$1" }
  ]
}
