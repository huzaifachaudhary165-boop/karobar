<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 9+ blocks plain HTTP by default, which is why a release APK pointed at
  a laptop on the same Wi-Fi fails with "Cannot reach the server".

  Rather than switching cleartext on everywhere, HTTPS stays mandatory and only
  the addresses used while developing are exempted. A deployed server is HTTPS,
  so nothing here loosens production.

  Testing against your own machine? Replace the LAN address below with the one
  `ipconfig` reports for your Wi-Fi adapter.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <!-- Android emulator's alias for the host machine -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <!-- Desktop / iOS simulator -->
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- This machine on the local network, for testing on a real phone -->
        <domain includeSubdomains="false">192.168.2.5</domain>
    </domain-config>
</network-security-config>
