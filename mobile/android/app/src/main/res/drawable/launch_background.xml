<?xml version="1.0" encoding="utf-8"?>
<!-- Splash shown before the Flutter engine is ready: brand orange with the
     white mark centred, so the handoff to SplashScreen is invisible. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/brand_primary" />
    <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/splash_mark" />
    </item>
</layer-list>
